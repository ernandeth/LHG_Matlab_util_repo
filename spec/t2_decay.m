function y=t2_decay(parms, time)
%function y=t2_decay(parms, time)
%        m0=parms(1);
%        R2=parms(2);
%        y=m0*exp(-R2 * time);
        m0=parms(1);
        R2=parms(2);
        y=m0*exp(-R2 * time);
return

